import 'package:flutter/material.dart';

ThemeData lightTheme =
    ThemeData(brightness: Brightness.light, primaryColor: Colors.amber);
ThemeData darkTheme =
    ThemeData(brightness: Brightness.dark, accentColor: Colors.amber);
